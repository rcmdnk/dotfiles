prefix_rule(
  pattern=["git", ["add", "commit", "diff"]],
  decision="allow",
)

prefix_rule(
  pattern=["uv", "run"],
  decision="allow",
)

prefix_rule(
  pattern=["pre-commit"],
  decision="allow",
)

prefix_rule(
  pattern=["prek"],
  decision="allow",
)
